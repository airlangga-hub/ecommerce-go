meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{url}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john@lennon.com",
    "password": "johnlennon"
  }
}

script:post-response {
  bru.setVar("user_token", res.body.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
